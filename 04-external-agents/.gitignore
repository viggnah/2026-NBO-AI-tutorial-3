# Local reference copy of the product docs, symlinked in for writing this
# module. Not part of the lab.
docs

# Written by crewai-agent/run.sh to track the process it started.
.run.pid

# Every .env variant holds live keys - backups, one-per-agent copies, the
# .env.old you leave behind while switching. Ignore them all and keep the
# single tracked exception explicit.
.env
.env.*
!.env.example
